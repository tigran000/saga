import * as actions from '../

const initalState = {
    titles: [],
    isFetching: false
}

const titlesHandler = (state = initalState, action) => {

    switch (action.type) {
        case TITLES.LOAD:
            return {

            }
    }
}

export default titlesHandler